import 'package:flutter/material.dart';

class View3 extends StatelessWidget{

  const View3();

  @override
  Widget build(BuildContext context) {

    return Container(
        alignment: Alignment.center,
        child: Text('TAB3', textScaler: TextScaler.linear(5)),
    );
  }
}